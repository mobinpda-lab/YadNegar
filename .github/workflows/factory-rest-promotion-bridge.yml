name: Factory REST Promotion Bridge

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
  workflow_run:
    workflows: ['YadNegar CI', 'YadNegar UI Evidence', 'YadNegar Android Build']
    types: [completed]

permissions:
  contents: read
  pull-requests: write
  actions: read

concurrency:
  group: factory-rest-promotion-bridge
  cancel-in-progress: false

jobs:
  promote:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v8
        with:
          script: |
            const { owner, repo } = context.repo;
            const pulls = await github.paginate(github.rest.pulls.list, {
              owner, repo, state: 'open', base: 'main', per_page: 100,
            });
            for (const pr of pulls) {
              if (!pr.draft) continue;
              const head = pr.head.sha;
              const runs = await github.paginate(github.rest.actions.listWorkflowRunsForRepo, {
                owner, repo, head_sha: head, event: 'pull_request', per_page: 100,
              });
              const ci = runs.filter(r => r.name === 'YadNegar CI' && r.status === 'completed')
                .sort((a,b) => b.id-a.id)[0];
              if (!ci || ci.conclusion !== 'success') continue;
              const base = ci.pull_requests?.find(p => p.number === pr.number)?.base?.sha;
              const main = (await github.rest.repos.getBranch({owner,repo,branch:'main'})).data.commit.sha;
              if (base !== main) continue;
              await github.request('PATCH /repos/{owner}/{repo}/pulls/{pull_number}', {
                owner, repo, pull_number: pr.number, draft: false,
              });
              core.notice(`#${pr.number}: promoted Draft -> Ready using REST after exact-head CI gate.`);
              return;
            }
            core.info('No eligible Draft PR requires REST promotion.');
